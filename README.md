# MateriaEngine

Paper/Folia 1.21.4 plugin for machine logic, intended to pair with CraftEngine content packs.

## Split

- Plugin: machine logic, GUI, recipes, data, Folia-safe scheduling.
- CraftEngine: items, blocks, models, textures, GUI icons.

## Build

```bash
./gradlew build
```
